@AGENTS.md

# Arc Forge AI Agent Notes

This project now uses self-owned runtime foundations:

- internal auth and account recovery
- internal PostgreSQL-backed AI worker
- internal WebSocket realtime engine
- provider-agnostic storage
- provider-agnostic AI runtime

Follow `AGENTS.md` and the `context/` documentation before making changes. Do not use removed hosted-runtime helper instructions for active implementation work.
